services:
  rendered-permissions:
    image: ghcr.io/glepzilla/shiki-cards-bot:${IMAGE_TAG:-latest}
    user: "0:0"
    entrypoint: ["/bin/sh", "-c"]
    command: ["mkdir -p /app/.cache/rendered /app/.cache/shikimori && chown -R 10001:10001 /app/.cache/rendered /app/.cache/shikimori"]
    volumes:
      - rendered:/app/.cache/rendered
      - shikimori-state:/app/.cache/shikimori
    restart: "no"

  bot:
    image: ghcr.io/glepzilla/shiki-cards-bot:${IMAGE_TAG:-latest}
    restart: unless-stopped
    depends_on:
      rendered-permissions:
        condition: service_completed_successfully
    env_file: .env
    environment:
      # The proxy runs on the Docker host and listens on the Docker bridge.
      PROXY_URL: http://host.docker.internal:7890
    extra_hosts:
      - host.docker.internal:host-gateway
    volumes:
      - rendered:/app/.cache/rendered
      - shikimori-state:/app/.cache/shikimori
    networks: [proxy]
    healthcheck:
      test: ["CMD", "/app/.venv/bin/python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    labels:
      - traefik.enable=true
      - traefik.http.routers.shiki.rule=Host(`shiki.glepzilla.ru`)
      - traefik.http.routers.shiki.entrypoints=websecure
      - traefik.http.services.shiki.loadbalancer.server.port=8080

volumes:
  rendered:
  shikimori-state:

networks:
  proxy:
    external: true
